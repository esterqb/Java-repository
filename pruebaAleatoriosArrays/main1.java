package pruebaAleatoriosArrays;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class main1 {
    /*
     * EJERCICIO 1: Realiza un programa que pinte una tableta de turrón con un bocado realizado de forma aleatoria.
     * El ancho y el alto de la tableta se pide por teclado.
     * El bocado se da alrededor del turrón, obviamente no se puede dar un bocado en medio de la tableta.
     * Ejemplo 1:
     * Introduzca la anchura de la tableta:
     * Introduzca la altura de la tableta:
     * "*****
     *  ***
     *  *****"
     *  */
    public static void main(String[] args) throws IOException {
       /* int ancho,alto;
        BufferedReader br= new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Introduzca la anchura de la tableta: ");
        ancho=Integer.parseInt(br.readLine());
        System.out.println("Introduzca la altura de la tableta: ");
        alto=Integer.parseInt(br.readLine());

        char[][] turron=new char[alto][ancho];



        for (int i=1;i<=alto;i++){
            for (int e=1;e<=ancho;e++){
                System.out.println(turron[i][e]='*');}
        }

        int coor1,coor2;
        int borde=Math.random();

        switch(bocado){
            case 0:
                coor1=0;
                coor2=Math.random(ancho);
                break;
            case 1:
                coor1=alto-1;
                coor2=Math.random(ancho);
                break;
            case 2:
                coor2=0;
                coor1=Math.random(alto);
                break;
            case 3:
                coor2=ancho-1;
                coor1=Math.random(alto);
                break;
        }

        boolean falta=false; //o true y en el if falta=false???

        if (turron[][]=='*'){
            turron[][]=' ';
            falta=true;
            }*/
        }


}
